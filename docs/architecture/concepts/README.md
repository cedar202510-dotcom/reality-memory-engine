# 架构探索（未冻结）

这里放**尚未成为工程口径**的架构探索。与 `product/concepts/` 的定位一致：
可以用于讨论和 review，**不得作为实现依据**，也不得被其他文档引用为已冻结结论。

| 文档 | 主题 | 状态 |
| --- | --- | --- |
| [07 Idea 存在性证明与加密撮合](07-Idea-Provenance-and-Encrypted-Matching.md) | idea/问题的承诺哈希、时间戳锚定、加密相似性撮合、链上托管结算 | 探索方案，结论为「仅做 Phase 0」 |

## 与 `architecture/` 顶层的区别

- 顶层：当前系统边界与分层方案，冲突时以其为准。
- 本目录：可行性判断和机制设计，**明确标注哪些不做、为什么不做**。

一份文档从本目录晋升到顶层，必须满足：范围已冻结、有验收标准、
被 `docs/README.md` 的导航表收录为「当前参考」以上状态。
