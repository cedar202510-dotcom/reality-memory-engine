fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
pillow>=10.3
pgvector>=0.3
python-multipart>=0.0.9
pytest>=8.2
pytest-asyncio>=0.23

# --- 可选：本地 CLIP 推理（仅 vision_provider=local 时需要；fake/http/none 路径不依赖） ---
# open-clip-torch>=2.26
# torch>=2.2  # 按目标平台官方指引安装（CPU/CUDA/ARM 轮子不同），本仓库不强依赖

# --- 可选：ASR sidecar 自建（仅 asr_provider=http 的服务端需要；平台本身只走 HTTP 不依赖） ---
# faster-whisper>=1.0
